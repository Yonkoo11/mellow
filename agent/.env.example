# Funder wallet (needs Sepolia ETH + USDT for funding agent wallets)
PRIVATE_KEY=0x...

# WDK seed phrase (derives client + worker wallets via BIP-44)
WDK_SEED_PHRASE=word1 word2 word3 ... word12

# Anthropic API key (for LLM-powered agent decisions)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Sepolia RPC (hardcoded default in constants.ts)
# RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
